---
const rawBase = import.meta.env.BASE_URL;
const base = rawBase.endsWith('/') ? rawBase.slice(0, -1) : rawBase;
const homeHref = `${base}/` || '/';
const pathname = Astro.url.pathname.replace(/\/$/, '') || homeHref;

const navItems = [
  { label: 'Home', href: homeHref },
  { label: 'Component Library', href: `${base}/component-library` },
  { label: 'Portfolio Index', href: `${base}/portfolio-index` },
];
---

<header class="w-full border-b border-neutral-200/80 bg-[#fcfbf9]/90 backdrop-blur-md dark:border-neutral-800/80 dark:bg-[#0e0e0e]/90 sticky top-0 z-50">
  <div class="w-full px-4 sm:px-6 md:px-8 lg:px-10 h-16 sm:h-18 flex items-center justify-between">
    <div class="flex items-center">
      <a
        id="navbar-logo"
        href={homeHref}
        aria-label="Home"
        class="text-base sm:text-lg md:text-xl font-black tracking-tight text-neutral-950 dark:text-white hover:opacity-80 transition-opacity font-heading cursor-pointer focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-[#52FF1A] rounded"
      >
        &lt;&nbsp;yovan&nbsp;/&gt;
      </a>
    </div>

    <div class="flex items-center gap-1.5 sm:gap-3">
      <!-- Desktop Navigation -->
      <nav aria-label="Main Navigation" class="hidden md:block">
        <ul class="flex items-center gap-1 sm:gap-2">
          {navItems.map((item) => {
            const itemPath = item.href.replace(/\/$/, '');
            const normalizedPathname = pathname.replace(/\/$/, '');
            const isActive = normalizedPathname === itemPath || (item.href !== homeHref && normalizedPathname.startsWith(itemPath));
            return (
              <li>
                <a
                  href={item.href}
                  class:list={[
                    'text-xs sm:text-sm font-medium px-3.5 py-2 rounded-md transition-colors focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-[#52FF1A]',
                    isActive
                      ? 'text-neutral-950 dark:text-white bg-neutral-100 dark:bg-neutral-900 font-semibold'
                      : 'text-neutral-600 hover:text-neutral-900 dark:text-neutral-400 dark:hover:text-white hover:bg-neutral-50 dark:hover:bg-neutral-900/50',
                  ]}
                  aria-current={isActive ? 'page' : undefined}
                >
                  {item.label}
                </a>
              </li>
            );
          })}
        </ul>
      </nav>

      <div class="hidden md:block h-4 w-px bg-neutral-200 dark:bg-neutral-800 mx-0.5"></div>

      <!-- Theme Toggle Button -->
      <button
        id="theme-toggle"
        type="button"
        aria-label="Toggle light/dark theme"
        aria-pressed="false"
        class="p-2 rounded-md text-neutral-600 dark:text-neutral-400 hover:text-neutral-950 dark:hover:text-white hover:bg-neutral-100 dark:hover:bg-neutral-900 transition-colors flex items-center justify-center cursor-pointer focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-[#52FF1A] focus-visible:ring-offset-2 dark:focus-visible:ring-offset-neutral-950"
      >
        <!-- Sun Icon (shown in dark mode to switch to light) -->
        <svg
          id="theme-toggle-sun"
          class="w-4 h-4 hidden"
          xmlns="http://www.w3.org/2000/svg"
          viewBox="0 0 24 24"
          fill="none"
          stroke="currentColor"
          stroke-width="2"
          stroke-linecap="round"
          stroke-linejoin="round"
        >
          <circle cx="12" cy="12" r="4"></circle>
          <path d="M12 2v2"></path>
          <path d="M12 20v2"></path>
          <path d="m4.93 4.93 1.41 1.41"></path>
          <path d="m17.66 17.66 1.41 1.41"></path>
          <path d="M2 12h2"></path>
          <path d="M20 12h2"></path>
          <path d="m6.34 17.66-1.41 1.41"></path>
          <path d="m19.07 4.93-1.41 1.41"></path>
        </svg>
        <!-- Moon Icon (shown in light mode to switch to dark) -->
        <svg
          id="theme-toggle-moon"
          class="w-4 h-4 hidden"
          xmlns="http://www.w3.org/2000/svg"
          viewBox="0 0 24 24"
          fill="none"
          stroke="currentColor"
          stroke-width="2"
          stroke-linecap="round"
          stroke-linejoin="round"
        >
          <path d="M12 3a6 6 0 0 0 9 9 9 9 0 1 1-9-9Z"></path>
        </svg>
      </button>

      <!-- Mobile Menu Button (hypebox style 2-bar animated transform) -->
      <button
        type="button"
        id="mobile-menu-toggle"
        aria-label="Toggle Mobile Navigation Menu"
        aria-expanded="false"
        aria-controls="mobile-menu-drawer"
        class="md:hidden flex flex-col justify-center items-center w-10 h-10 gap-1.5 cursor-pointer relative z-50 rounded focus:outline-none focus-visible:ring-2 focus-visible:ring-[#52FF1A]"
      >
        <span
          id="mobile-bar-1"
          class="w-6 h-[2px] bg-neutral-950 dark:bg-white transition-transform duration-300 origin-center"
        ></span>
        <span
          id="mobile-bar-2"
          class="w-6 h-[2px] bg-neutral-950 dark:bg-white transition-transform duration-300 origin-center"
        ></span>
      </button>
    </div>
  </div>

  <!-- Mobile Drawer Menu (hypebox style backdrop blur & typography) -->
  <div
    id="mobile-menu-drawer"
    aria-hidden="true"
    class="md:hidden fixed top-16 sm:top-18 left-0 w-full bg-[#fcfbf9]/98 dark:bg-[#0e0e0e]/98 backdrop-blur-xl border-b border-neutral-200/80 dark:border-neutral-800/80 shadow-2xl transition-all duration-300 ease-out -translate-y-4 opacity-0 pointer-events-none z-40 overflow-hidden"
  >
    <div class="px-6 py-8 flex flex-col gap-4">
      {navItems.map((item) => {
        const itemPath = item.href.replace(/\/$/, '');
        const normalizedPathname = pathname.replace(/\/$/, '');
        const isActive = normalizedPathname === itemPath || (item.href !== homeHref && normalizedPathname.startsWith(itemPath));
        return (
          <a
            href={item.href}
            class:list={[
              'mobile-nav-link font-heading font-bold text-2xl uppercase tracking-tight transition-colors flex items-center justify-between',
              isActive
                ? 'text-neutral-950 dark:text-white font-black'
                : 'text-neutral-600 dark:text-neutral-400 hover:text-[#52FF1A] dark:hover:text-[#52FF1A]',
            ]}
            aria-current={isActive ? 'page' : undefined}
          >
            <span>{item.label}</span>
            {isActive && (
              <span class="w-2 h-2 rounded-full bg-[#52FF1A]"></span>
            )}
          </a>
        );
      })}

      <div class="mt-4 pt-6 border-t border-neutral-200/80 dark:border-neutral-800/80 flex items-center gap-6 text-xs text-neutral-500 dark:text-neutral-400 font-mono">
        <a
          href="https://github.com/navoyovan"
          target="_blank"
          rel="noopener noreferrer"
          class="hover:text-neutral-950 dark:hover:text-white transition-colors"
        >
          GitHub ↗
        </a>
        <a
          href="https://linkedin.com/in/yovan-g-d-22379ba7/"
          target="_blank"
          rel="noopener noreferrer"
          class="hover:text-neutral-950 dark:hover:text-white transition-colors"
        >
          LinkedIn ↗
        </a>
      </div>
    </div>
  </div>
</header>

<script>
  const setupLogoScroll = () => {
    const logo = document.getElementById('navbar-logo');
    if (!logo) return;

    logo.addEventListener('click', (e) => {
      const rawBase = import.meta.env.BASE_URL;
      const base = rawBase.endsWith('/') ? rawBase.slice(0, -1) : rawBase;
      const homePath = (base || '').replace(/\/$/, '');
      const currentPath = window.location.pathname.replace(/\/$/, '');

      if (currentPath === homePath || currentPath === '') {
        e.preventDefault();
        window.scrollTo({ top: 0, behavior: 'smooth' });
      }
    });
  };

  const setupThemeToggle = () => {
    const toggleBtn = document.getElementById('theme-toggle');
    const sunIcon = document.getElementById('theme-toggle-sun');
    const moonIcon = document.getElementById('theme-toggle-moon');
    if (!toggleBtn || !sunIcon || !moonIcon) return;

    const updateIcons = () => {
      const isDark = document.documentElement.classList.contains('dark');
      toggleBtn.setAttribute('aria-pressed', String(isDark));
      if (isDark) {
        sunIcon.classList.remove('hidden');
        moonIcon.classList.add('hidden');
      } else {
        sunIcon.classList.add('hidden');
        moonIcon.classList.remove('hidden');
      }
    };

    updateIcons();

    toggleBtn.onclick = () => {
      const isDark = document.documentElement.classList.contains('dark');
      if (isDark) {
        document.documentElement.classList.remove('dark');
        localStorage.setItem('theme', 'light');
      } else {
        document.documentElement.classList.add('dark');
        localStorage.setItem('theme', 'dark');
      }
      updateIcons();
    };
  };

  const setupMobileMenu = () => {
    const toggleBtn = document.getElementById('mobile-menu-toggle');
    const drawer = document.getElementById('mobile-menu-drawer');
    const bar1 = document.getElementById('mobile-bar-1');
    const bar2 = document.getElementById('mobile-bar-2');
    const mobileLinks = document.querySelectorAll('.mobile-nav-link');

    if (!toggleBtn || !drawer || !bar1 || !bar2) return;

    let isOpen = false;

    const setMenuState = (open: boolean) => {
      isOpen = open;
      toggleBtn.setAttribute('aria-expanded', open.toString());
      drawer.setAttribute('aria-hidden', (!open).toString());

      if (open) {
        drawer.classList.remove('-translate-y-4', 'opacity-0', 'pointer-events-none');
        drawer.classList.add('translate-y-0', 'opacity-100', 'pointer-events-auto');
        bar1.style.transform = 'translateY(4px) rotate(45deg)';
        bar2.style.transform = 'translateY(-4px) rotate(-45deg)';
      } else {
        drawer.classList.remove('translate-y-0', 'opacity-100', 'pointer-events-auto');
        drawer.classList.add('-translate-y-4', 'opacity-0', 'pointer-events-none');
        bar1.style.transform = 'none';
        bar2.style.transform = 'none';
      }
    };

    toggleBtn.onclick = (e) => {
      e.stopPropagation();
      setMenuState(!isOpen);
    };

    // Close when clicking links
    mobileLinks.forEach((link) => {
      link.addEventListener('click', () => {
        setMenuState(false);
      });
    });

    // Close on Escape key
    const onKeyDown = (e: KeyboardEvent) => {
      if (e.key === 'Escape' && isOpen) {
        setMenuState(false);
        toggleBtn.focus();
      }
    };
    document.removeEventListener('keydown', onKeyDown);
    document.addEventListener('keydown', onKeyDown);

    // Close when clicking outside
    const onDocClick = (e: MouseEvent) => {
      if (isOpen && !drawer.contains(e.target as Node) && !toggleBtn.contains(e.target as Node)) {
        setMenuState(false);
      }
    };
    document.removeEventListener('click', onDocClick);
    document.addEventListener('click', onDocClick);

    // Auto-close on resize to md screen size (768px+)
    const onResize = () => {
      if (window.innerWidth >= 768 && isOpen) {
        setMenuState(false);
      }
    };
    window.removeEventListener('resize', onResize);
    window.addEventListener('resize', onResize);
  };

  const initNavbar = () => {
    setupLogoScroll();
    setupThemeToggle();
    setupMobileMenu();
  };

  if (document.readyState === 'loading') {
    document.addEventListener('DOMContentLoaded', initNavbar);
  } else {
    initNavbar();
  }
  document.addEventListener('astro:page-load', initNavbar);
</script>
