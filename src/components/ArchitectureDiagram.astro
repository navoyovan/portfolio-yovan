---
interface Props {
  class?: string;
}

const { class: className = "" } = Astro.props;
---

<div class:list={[
  "w-full text-xs text-neutral-400 dark:text-neutral-500 font-mono italic",
  className
]}>
  *empty for now
</div>
